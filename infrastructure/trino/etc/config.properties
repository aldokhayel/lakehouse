# infrastructure/trino/etc/config.properties
# Trino coordinator configuration.
# This node acts as both coordinator and worker (suitable for a single-node
# local deployment). Adjust node-scheduler.include-coordinator=false if you
# add dedicated worker nodes.

coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080
discovery.uri=http://localhost:8080
