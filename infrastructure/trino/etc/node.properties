# infrastructure/trino/etc/node.properties
# Identifies this Trino node within the cluster.
# node.id must be unique per node; a fixed UUID is fine for single-node setups.

node.environment=production
node.id=ffffffff-ffff-ffff-ffff-ffffffffffff
node.data-dir=/data/trino
