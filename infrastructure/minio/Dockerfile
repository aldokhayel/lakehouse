# infrastructure/minio/Dockerfile
# MinIO runs well as-is from the official image.
# Bucket creation is handled separately by the minio-init service
# (see docker-compose.yml) using the MinIO Client (mc) image.
# This Dockerfile is kept as a placeholder for any future customisation.

FROM minio/minio:latest
# MinIO runs as-is; bucket creation is handled by minio-init service
